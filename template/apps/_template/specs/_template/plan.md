# SPEC-APP-{XXX}: 前端执行计划

## 架构总览

```text
{组件树 + 数据流向}
```

## 改动步骤

### Step 1: {组件名} — {改动}

**文件**: `apps/{app-name}/src/components/{path}`

{骨架说明}

### Step 2: API 对接

**文件**: `apps/{app-name}/src/services/api.ts`

{接口签名}

## 文件修改清单

| # | 文件 | 操作 | 预期工时 |
|:-:|:----|:----|:-------:|
| 1 | `apps/{name}/src/{path}` | 新增 | 30min |

## 注意事项

- {UI 细节}
- {响应式规则}

## 回滚方案

{如何回退}
