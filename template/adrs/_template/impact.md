# 影响与注意事项 — {一次大型变动的完整新结构}

> **所属 ADR**: `adr-{YYYYMMDD}-{变动名}`
> **维护者**: @{name}
> **最新更新**: {YYYY-MM-DD}
> **本份文档职责**: 本次架构变动影响的关联模块、部署依赖、配置项、回滚方案。

---

## 一、关联模块影响

| 模块 | 影响 | 说明 |
|:----|:----|:-----|
| `apps/{name}` | 前端对接 | 需要调整 API 调用 |
| `businesses/{name}` | 后端核心 | 直接修改 |
| `sprints/sp-NNN-*/specs/spec-XXX_*` | 受影响的规格 | 实现方案需跟随本次架构 |

## 二、部署依赖

- {部署环境要求}
- {迁移/初始化步骤}

## 三、配置项

| 配置 | 值/示例 | 说明 |
|:----|:--------|:-----|
| {config key} | {value} | {用途} |

## 四、回滚方案

- {回滚步骤}
- {风险点}

---

> **给 AI 的提示**: 本次变更是**大型架构变动才出一份完整 adr 文件夹**（本份 + `architecture.md` + `data-model.md`）。Dev 与 AI 完成一次架构交流后即产出。小改动（bugfix / 按钮文案）不写 adr。
