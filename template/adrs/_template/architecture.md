# 架构总览 — {一次大型变动的完整新结构}

> **所属 ADR**: `adr-{YYYYMMDD}-{变动名}`（一次大型架构变动 = 一个 adr 文件夹，整份设计）
> **维护者**: @{name}
> **最新更新**: {YYYY-MM-DD}
> **状态**: 草稿 → 生效 → 已废弃
> **本份文档职责**: 整个系统的技术架构总览（上下文 / 组件 / 流程 / 技术选型）。**技术设计只出现在 adrs/，其他目录不承载技术设计。**

---

## 一、系统上下文

```text
{ASCII 架构图 — 本设计涉及哪些组件、数据流向、外部依赖}
{例如:}
用户 → 前端 (apps/web)
         │ HTTP
         ▼
API 网关 → businesses/user-service → DB
              │
              └──→ 外部 API (支付/认证)
```

## 二、组件与职责

| 组件 | 职责 | 所属代码域 | 说明 |
|:----|:-----|:----------|:-----|
| {组件名} | {核心职责} | `apps/` / `businesses/` / `tools/` | {关键说明} |

## 三、关键流程

```text
{核心流程的 ASCII 序列图}
{例如:}
用户操作 → API 路由 → 业务逻辑 → DB 写入 → 返回结果
```

## 四、技术选型（整系统）

| 层 | 组件 | 选型 | 理由 |
|:---|:----|:-----|:----:|
| {层名} | {组件名} | {选定的技术} | {关键理由} |

> 技术选型是**整个系统**的，不是某个 spec 的。spec 内只写基于此选型的实现方案，不重复做技术决策。

---

> **给 AI 的提示**: 这是系统架构总览。AI 首次进入项目先读 `adrs/` 所有文件夹理解全局，再进具体 sprint 读 spec。一次大型架构变动 = 一个 adr 文件夹（本份 + `data-model.md` + `impact.md`），**不按技术点拆多个 adr**。
