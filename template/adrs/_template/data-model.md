# 数据模型 — {一次大型变动的完整新结构}

> **所属 ADR**: `adr-{YYYYMMDD}-{变动名}`
> **维护者**: @{name}
> **最新更新**: {YYYY-MM-DD}
> **本份文档职责**: 整个系统的数据模型（实体 / 关系 / 表结构）。**数据设计只出现在 adrs/，spec 内只写落地实现，不重复定义模型。**

---

## 一、实体关系总览

```mermaid
erDiagram
    {ENTITY-A} ||--o{ {ENTITY-B} : {关系说明}
    {ENTITY-B} }o--|| {ENTITY-C} : {关系说明}
```

## 二、核心实体

| 实体 | 关键字段 | 说明 |
|:----|:--------|:-----|
| {实体名} | {字段列表} | {业务含义} |

## 三、表结构 / Schema

```sql
-- {表名}
CREATE TABLE {table_name} (
  id          UUID PRIMARY KEY,
  {column}    {type} NOT NULL,
  -- 关键约束
);
```

## 四、共享字典

| 字典项 | 定义 | 使用方 |
|:------|:-----|:-------|
| {枚举/状态值} | {取值范围与含义} | {哪些 spec/模块使用} |

---

> **给 AI 的提示**: 数据模型是跨 spec 共享的**唯一权威定义**。spec 引用本文件时用相对链接，不复制表结构（SSOT）。改数据模型 = 一次大型变动 → 更新本 ADR 文件夹（影响范围写进 `impact.md`）。
