# adr-{NNN}: {标题}

> **类型**: 架构设计 / 决策记录
> **维护者**: @{name}
> **最新更新**: {YYYY-MM-DD}
> **状态**: 草稿 → 生效 → 已废弃
> **影响范围**: {列举受影响的模块}

---

## 一、系统上下文

```text
{ASCII 架构图 — 本设计涉及哪些组件、数据流向、外部依赖}
{例如:}
用户 → 前端 (apps/web)
         │ HTTP
         ▼
API 网关 → businesses/user-service → DB
              │
              └──→ 外部 API (支付/认证)
```

## 二、核心设计

### 2.1 数据模型

```typescript
// 核心实体和关系
interface Entity {
  id: string;
  // 关键字段
}
```

### 2.2 关键流程

```text
{核心流程的 ASCII 序列图}
{例如:}
用户操作 → API 路由 → 业务逻辑 → DB 写入 → 返回结果
```

### 2.3 技术选型

| 组件 | 选型 | 理由 |
|:----|:-----|:----:|
| {组件名} | {选定的技术} | {关键理由} |

## 三、关联模块

| 模块 | 影响 | 说明 |
|:----|:----|:-----|
| `apps/{name}` | 前端对接 | 需要调整 API 调用 |
| `businesses/{name}` | 后端核心 | 直接修改 |

## 四、注意事项

- {部署依赖}
- {配置项}
- {回滚方案}

---

> **给 AI 的提示**: 本文档是系统的架构设计视图。AI 在执行前应先通读 `adrs/` 下所有文件理解全局。写新的架构设计时，更新现有 adr 或新增一个文件。
