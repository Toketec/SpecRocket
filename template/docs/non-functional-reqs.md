# {项目名} — 非功能需求

> **负责人**: PM / 产品经理
> **维护**: 每轮 sprint 评审时审视
> **用途**: 定义产品的质量属性要求，Dev 据此在 specs 中做技术选型。这不写技术方案，只写业务要求。
> **必填规则**: 即使项目极简，也**必须保留此文件**。无实质内容时写一行占位说明为什么不需要，不可遗漏或删除。

---

## 🚀 快速判断（首次编辑时先填此节）

**本项目是否有非功能需求（性能/SLA/安全/合规）？** {有 / 无}

| 判断结果 | 操作 |
|:--------|:-----|
| **有** | 继续填写下方正文，根据产品类型勾选对应指标 |
| **无（纯脚本/一次性工具/本地工具）** | 在下一行写入占位说明后，本文档完成：`本项目为无用户交互的脚本工具，无特殊非功能需求要求。` |

---

## 一、性能 (Performance)

| 场景 | 目标值 | 测量方式 | 松紧 |
|:----|:------|:--------|:----:|
| 页面首次加载 | ≤ 3s | Lighthouse / 真实用户监控 | 硬性 |
| 页面切换（SPA） | ≤ 1s | Chrome DevTools | 软性 |
| API 响应（P95） | ≤ 500ms | 服务端日志 | 硬性 |
| API 响应（P99） | ≤ 2s | 服务端日志 | 软性 |
| 并发用户数 | ≥ {N} | 压测 | 硬性 |

## 二、可用性 (Availability & SLA)

| 指标 | 目标 |
|:----|:-----|
| 系统可用性 | 99.{N}% |
| 计划内停机窗口 | {时间段} |
| 最大故障恢复时间 (RTO) | {N} min |
| 最大数据丢失 (RPO) | {N} min |
| 错误率上限 | < 1% 的请求返回 5xx |

## 三、安全 (Security)

- {认证方式：如邮箱+密码、OAuth、SSO}
- {密码策略：如 8 位以上、含特殊字符}
- {敏感数据：如用户手机号、身份证需加密存储}
- {接口防刷：如 API 限流、验证码}
- {日志审计：如关键操作需记录操作人、时间、内容}

## 四、兼容性 (Compatibility)

| 维度 | 要求 |
|:----|:-----|
| 浏览器 | Chrome ≥ {版本}、Safari ≥ {版本}、Edge ≥ {版本} |
| 移动端 | iOS ≥ {版本}、Android ≥ {版本} |
| 屏幕尺寸 | {最小宽度} ~ {最大宽度} 自适应 |
| 网络环境 | {弱网/断网/慢速 3G} 下需要 {降级方案} |

## 五、数据与合规 (Data & Compliance)

- {数据存储地：如中国大陆、AWS Singapore}
- {数据保留策略：如用户日志保留 90 天}
- {合规要求：如 GDPR、个人信息保护法}
- {数据导出：用户能否导出自己的数据？格式？}

## 六、可观测性 (Observability)

### 埋点需求

| 事件 | 触发时机 | 上报字段 |
|:----|:--------|:--------|
| {事件名} | {触发条件} | {字段列表} |
| {事件名} | {触发条件} | {字段列表} |

### 监控告警

- {关键指标的监控阈值}
- {告警通知方式：如钉钉、邮件、PagerDuty}

## 七、国际化与本地化 (i18n & L10n)

- {支持语言：如简体中文、繁体中文、英文}
- {多币种：如 CNY、USD}
- {时区处理策略}

---

> **评审**: PM + TL 确认非功能需求可落地后，进入冲刺规划。Dev 在 `adrs/`（架构变动设计）和 `specs/` 中提供技术方案满足这些需求。
