# Sprint {NNN}: {冲刺名称} — UI 设计说明

> **负责人**: PM
> **定位**: 承载**随版本变动的 UI 具体形态**——页面结构、布局、内容、框线图、文字说明。每个冲刺一份，随版本演进。
> **与 `visual-design.md` 的关系**: `docs/visual-design.md` 定义**全局唯一、与版本无关**的审美边界（颜色/大小/设计语言/风格/动效/间隔）；本文档定义**本版本**的页面具体形态（哪些页面、什么布局、什么内容、怎么交互）。
> **与 `prototype.html` 的关系**: `prototype.html` 是**可点击交互原型**（只做演示）；本文档是**设计说明书**（说清楚每页的结构/内容/文案/状态）。原型中放不下的文字说明、线框、页面清单都在这里，**两者一一对应**。

---

## 一、页面清单

| # | 页面 | 标识（对应原型） | 一句话说明 |
|:-:|:----|:---------|:----|
| 1 | {页面名} | page1 | {这个页面解决什么} |
| 2 | {页面名} | page2 | ... |

---

## 二、页面设计（每页一节）

### 2.1 {页面名}

**目的**: {这个页面解决什么、用户在此完成什么}

**布局（线框）**:

```text
┌──────────────────────────────┐
│ 头部: {标题} + {操作按钮}      │
├──────────────────────────────┤
│ 内容区:                       │
│  ├─ {区块/卡片}               │
│  │   ├─ {字段/内容}           │
│  │   └─ {字段/内容}           │
│  └─ 空状态: {提示语 + 引导操作} │
└──────────────────────────────┘
```

**内容与文案**:

| 区块 | 内容/字段 | 文案/示例 |
|:----|:--------|:--------|
| {头部} | {标题、操作} | {具体文案} |
| {内容区} | {卡片/列表字段} | {示例数据} |

**状态与提示**:

| 状态 | 表现 | 提示语 |
|:----|:----|:------|
| loading | {转圈/骨架屏} | {加载中...} |
| empty | {空状态插画/图标} | {暂无内容 + 引导操作} |
| error | {错误提示} | {失败文案 + 重试} |
| success | {成功反馈} | {toast/跳转} |

**交互说明**: {点击/跳转/弹窗/表单填写等行为，与 prototype.html 中的演示对应}

---

## 三、全局交互与文案说明

- {跨页面交互：导航切换、弹窗复用、toast 规则}
- {本版本所有按钮/提示/空状态文案一览}

> **维护规则**: 改文案/布局 → 改本文档 + 同步 `prototype.html`；原型只做交互演示，**不在原型里写说明文字**（说明都在本文档）。
